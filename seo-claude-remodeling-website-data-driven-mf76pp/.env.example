# Sign up at https://resend.com, verify a sending domain (or use their
# shared sandbox sender for testing), and create an API key.
RESEND_API_KEY=

# Where contact-form submissions are sent. Defaults to data/company.ts's
# `email` field if unset.
CONTACT_TO_EMAIL=
